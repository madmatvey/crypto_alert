<!DOCTYPE html>
<html>
  <head>
    <title>CryptoAlert</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", preload: false %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav style="padding: 1rem; background: var(--surface); border-bottom: 1px solid var(--border);">
      <%= link_to "Alerts", alerts_path, class: "button" %>
      <%= link_to "Channels", notification_channels_path, class: "button" %>
    </nav>

    <%= render "shared/flash" %>

    <main style="max-width: 960px; margin: 0 auto; padding: 1rem;">
      <%= yield %>
    </main>
  </body>
</html>
